import Filters from './Filters.jsx';

const Header = () => {
    return (
        <header>
            <h1>React Shop Example</h1>
            <Filters></Filters>
        </header>
    );
}

export default Header;
